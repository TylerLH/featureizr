var should = require('chai').should();
var featureizr = require('../');

describe("Featureizr", function() {
  describe("#handle", function() {
    it("should do the thing when something happens", function() {
      var foo = "bar";
      foo.should.equal("bar");
    });
  });
});
